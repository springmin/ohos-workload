bin/
obj/
src/**/bin/
src/**/obj/
# Packs with binary content are laid out on demand by scripts/prepare-packs.sh
packs/Microsoft.NETCore.App.Runtime.openharmony-arm64/
packs/**/ref/**/*.dll
packs/**/runtimes/**/*.dll
packs/**/runtimes/**/*.so
packs/**/hosts/**/*.so
# Generated artifacts: workload packs, bundles and signing material
.feed/
dist/
.signing/
# WIP: managed pixel harness (needs the canvas backend getters, see docs)
